num_dict = {'one': 1, 'two': 2, 'three': 3}

length = len(num_dict)
print("Length of dictionary: " + str(length))

keys_list = list(num_dict.keys())
print("List of keys: " + str(keys_list))

values_list = list(num_dict.values())
print("List of values: " + str(values_list))